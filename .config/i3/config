# load monitor settings
exec_always --no-startup-id xrandr --output eDP-1 --off --output HDMI-1 --primary --mode 1920x1080 --refresh 120.40 --pos 0x0 --rotate normal

# load wallpaper
exec_always --no-startup-id feh --bg-fill /home/stew/Pictures/3.png

# set mod
set $mod Mod4

# font
font pango:FuraCode NF 9

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# open a terminal
bindsym $mod+Return workspace $workspaceT; exec kitty
bindsym $mod+Shift+Return exec kitty

# open chrome
bindsym $mod+Shift+g exec google-chrome
bindsym $mod+g workspace $workspaceB; exec google-chrome


# kill focused window
bindsym $mod+Shift+q kill

# open rofi
bindsym $mod+d exec --no-startup-id rofi -show drun

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+x split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

set $workspaceT ""
set $workspaceB ""
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws5 "6"
set $ws5 "7"
set $ws5 "8"
set $workspaceA ""

# switch to workspace
bindsym $mod+1 workspace $workspaceT
bindsym $mod+2 workspace $workspaceB
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+9 workspace $workspaceA

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspaceT
bindsym $mod+Shift+2 move container to workspace $workspaceB
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws5
bindsym $mod+Shift+7 move container to workspace number $ws5
bindsym $mod+Shift+8 move container to workspace number $ws5
bindsym $mod+Shift+9 move container to workspace $workspaceA

assign [class="jetbrains-studio"] $workspaceA

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+Control+e exec i3-msg exit

# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym j resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# set window style border and name
for_window [class=".*"] border pixel 2

set $bgcolor        #1d2021
set $border         #8ec07c
set $text           #ebdbb2
set $urgent         #cc241d
set $unfocus        #d65d0e

# gaps and borders colors
# Property Name          Border   BG       Text     Indicator 
client.focused           $border  $border  $text    $border
client.focused_inactive  $unfocus $unfocus $text    $unfocus
client.unfocused         $unfocus $unfocus $text    $unfocus
client.placeholder       $unfocus $unfocus $text    $unfocus
client.urgent            $urgent  $urgent  $text    $urgent
client.background        $unfocus

# gaps and borders
gaps inner 4
gaps outer 7

bar {
    font pango:DejaVu Sans Mono, Font Awesome 6 Free Regular 11
    position top
    separator_symbol "  "
    status_command i3status --config ~/.config/i3status/i3status.conf
colors {
            background #1d2021
            statusline #ebdbb2
            separator  #fabd2f
            #                  border     background   text
            focused_workspace  #1d2021    #1d2021      #8ec07c
            inactive_workspace #1d2021    #1d2021      #7c6f64
            urgent_workspace   #1d2021    $urgent      $text
          }
}
